import React, { Component } from 'react'
import { Text, View } from 'react-native'

import MainStackNavigator from '../navigations/mainStackNavigator';

export default function GotStarted () {
    return <MainStackNavigator />
}
